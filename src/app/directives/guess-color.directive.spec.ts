import { GuessColorDirective } from './guess-color.directive';

describe('GuessColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GuessColorDirective();
    expect(directive).toBeTruthy();
  });
});
